# SEC-002: Example for local development. In production, use strong credentials.
DATABASE_URL="postgresql+psycopg2://user:password@localhost:5432/auth_db"

# SEC-001: CRITICAL: This key is used for signing JWT tokens.
# GENERATE A STRONG, UNIQUE KEY FOR PRODUCTION.
# Example: openssl rand -hex 32
SECRET_KEY="your-super-secret-key-here-change-this-in-production"

ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CQ-001: Set the environment. 'development' allows auto-migrations.
# In 'production' or 'testing', migrations should be handled externally (e.g., Alembic).
ENVIRONMENT="development"
